<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoodDetection"
        tools:targetApi="31">

        <activity android:name=".CharacterActivity" />
        <activity android:name=".SecondQuestion" />
        <activity android:name=".ThirdQuestion" />
        <activity android:name=".FourthQuestion" />
        <activity android:name=".NotificationPage" />
        <activity android:name=".FirstQuestion" />
        <activity android:name=".DailyGame" />
        <activity android:name=".MapActivity"/>
        <activity android:name=".TipsActivity"/>
        <activity android:name=".SpotifyPage" />

        <activity
            android:name=".LogInPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
